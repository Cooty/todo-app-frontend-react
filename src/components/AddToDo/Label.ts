import styled from '@emotion/styled';

const Label = styled.label`
    flex-basis: 100%;
    flex-shrink: 0;
    margin-bottom: 0.4rem;
    font-weight: bold;
    display: block;
    font-size: 2rem;
    cursor: pointer;
`;

export default Label;